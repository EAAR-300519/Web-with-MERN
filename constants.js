const DB_USER = "admin";
const DB_PASSWORD = "admin";

const API_VERSION = "v1";
const IP_SERVER = "localhost";

const JWT_SECRET_KEY = "prueba123";

module.exports = {
  DB_USER,
  DB_PASSWORD,
  API_VERSION,
  IP_SERVER,
  JWT_SECRET_KEY,
};
